apiVersion: v1
kind: Service
metadata:
  name: aws-tracker-service
  labels:
    app: aws-tracker-backend
spec:
  type: LoadBalancer
  selector:
    app: aws-tracker-backend
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8000
